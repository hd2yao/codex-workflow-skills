interface:
  display_name: "线程健康守卫"
  short_description: "判断长线程是否高风险并接续到新线程"
  default_prompt: "Use $codex-thread-health-guard to decide whether this Codex thread should continue in a clean new thread."
